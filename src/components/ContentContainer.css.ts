import { style } from 'typestyle';

export const className = style({
  width: '100%',
  background: '#121212',
  borderRadius: '6px',
  borderColor: '#000',
  margin: '0 6px 6px 0',
});

export const statusClass = style({
  color: '#999',
});
