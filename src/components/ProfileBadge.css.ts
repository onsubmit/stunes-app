import { style } from 'typestyle';

export const className = style({
  display: 'flex',
  flexDirection: 'row',
  alignItems: 'center',
  gap: '10px',
});

export const profilePhoto = style({
  width: '40px',
  height: '40px',
  borderRadius: '50%',
});
