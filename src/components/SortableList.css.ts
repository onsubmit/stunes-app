import { style } from 'typestyle';

export const className = style({
  flex: 1,
  maxHeight: '400px',
  overflowY: 'auto',
  padding: '6px',
  marginBottom: '6px',
});

export const listClass = style({
  listStyleType: 'none',
  padding: 0,
});
